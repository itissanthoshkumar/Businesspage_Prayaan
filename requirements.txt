fastapi>=0.110
# <0.50: Starlette 0.50 removed the legacy TemplateResponse(name, ctx)
# calling convention this codebase uses; pin until call sites migrate to
# TemplateResponse(request, name, ctx).
starlette>=0.47,<0.50
uvicorn[standard]>=0.27
pymongo>=4.6
jinja2>=3.1
